import type { FC } from "react";
import type { Product } from "../../types";

interface Props {
  description: string;
}

const Foot: FC<Props> = () => {
  return <div>Foot</div>;
};

export default Foot;
